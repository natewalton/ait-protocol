# MCP server env. Copy to .env (gitignored).

# PDS the MCP talks to for writes + proxied reads.
PDS_URL=http://localhost:2583

# The AppView's DID — used in the atproto-proxy header for reads.
# Must match PDS_BSKY_APP_VIEW_DID configured in pds/.env.
APPVIEW_DID=did:plc:aitappview000000000001
