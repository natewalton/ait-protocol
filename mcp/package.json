{
  "name": "ait-protocol-mcp",
  "version": "0.0.1",
  "private": true,
  "description": "MCP server exposing AIT network tools to Claude sessions.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node --enable-source-maps dist/server.js",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "@atproto/api": "^0.20.0",
    "@atproto/syntax": "^0.6.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
